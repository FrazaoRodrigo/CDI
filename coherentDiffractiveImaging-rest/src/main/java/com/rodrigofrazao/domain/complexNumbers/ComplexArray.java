package com.rodrigofrazao.domain.complexNumbers;

import com.rodrigofrazao.domain.image.ComplexImage;
import com.rodrigofrazao.domain.image.InlineComplexImage;

import java.util.ArrayList;

public class ComplexArray {


    public static Complex[] buildFromRowsList(ArrayList<Complex[]> rows) {

        int height = rows.size();
        Complex[] result = new Complex[rows.get(0).length * height];
        for (int i = 0;i<height;i++){
            result[i]
        }
    }
}
